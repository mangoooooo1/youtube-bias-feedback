# python -m pip install -r requirements.txt
# Python 3.11 기준. pingouin이 나머지(numpy/pandas/scipy/statsmodels/matplotlib/tabulate)를
# 의존성으로 함께 끌어오므로 pingouin만 설치해도 대부분 충족되지만, 재현성을 위해
# 실제 이 리허설을 실행·검증한 버전을 전부 고정한다.
numpy==1.26.4
pandas==3.0.1
scipy==1.17.1
statsmodels==0.15.0
pingouin==0.6.1
matplotlib==3.11.2
tabulate==0.10.0
