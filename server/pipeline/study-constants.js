// 이 연구(ViewLens)의 기간 캘린더 상수 — 서버 여러 파일이 동일한 값을 참조해야 하는 곳

const TOTAL_DAYS = 12;
const DAYS_PER_PERIOD = 4;
const BASELINE_DAYS = 4;

module.exports = { TOTAL_DAYS, DAYS_PER_PERIOD, BASELINE_DAYS };
