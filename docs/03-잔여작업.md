# 잔여 작업 구조

1차 개발 보고서(2026.06.08) 이후 남은 개발 작업을 정리한다.
Epic 1–5의 기본 구현은 완료되었으며, 이 문서는 배포 전 완성, 고도화, 연구 운영 작업을 다룬다.

---

## Epic 6: 피드백 고도화

- 설명: 현재 카테고리 분포 기반의 범용 피드백을 연구 의도에 맞는 개인화 피드백으로 개선한다. 편향 변화 추세와 편중 경고를 반영하여 사용자가 시청 다양성 변화를 체감할 수 있도록 한다.

---

### Story 6-1: 시청 패턴 변화에 기반한 연구 맞춤형 피드백을 생성할 수 있다

- 설명: Gemini 프롬프트를 개선하여 전 세션 대비 entropy 변화, 특정 카테고리 편중 경고, 다양성 탐색 권장 등 연구 목적에 부합하는 구체적인 피드백을 생성한다.
- Priority: High
- Owner: FE
- Definition of Done: 피드백이 전 세션 대비 entropy 변화 방향을 반영하고, 단일 카테고리 70% 이상 편중 시 다양성 탐색 권장 문구가 포함된다.
- Dependencies: Story 2-4 완료

#### Tasks

- [ ] `background.js`에서 분석 시 직전 세션 entropy 조회 및 전달
  - Priority: High / Owner: FE
  - 내용: `analyzeSession()`에서 `getAllSessions()`로 직전 분석 완료 세션의 entropy를 조회하여 `buildPrompt()`에 `prevEntropy`로 전달
  - DoD: `analyzeSession()` 호출 시 직전 세션의 entropy 값이 프롬프트 생성 함수에 전달된다.
  - Dependencies: 없음
  - 예상 소요: 1~2h

- [ ] `buildPrompt()`에 entropy 변화량 및 편중 경고 반영
  - Priority: High / Owner: FE
  - 내용: `llm.js`의 `buildPrompt()`에 `prevEntropy` 파라미터 추가. 전 세션 대비 Δ entropy와 최다 카테고리 비율이 70% 이상일 때 편중 경고 신호를 프롬프트에 포함
  - DoD: 편향이 심화된 세션과 다양해진 세션에서 각기 다른 방향의 피드백이 생성된다.
  - Dependencies: `prevEntropy` 전달 완료
  - 예상 소요: 2~3h

- [ ] `generateFallbackReview()` 개선
  - Priority: Medium / Owner: FE
  - 내용: Gemini API 실패 시 사용되는 `generateFallbackReview()`에도 entropy 변화 방향 반영. "다양한 카테고리 탐색해보세요" 수준에서 편향 변화를 언급하는 구체적인 문구로 개선
  - DoD: 폴백 피드백도 편향 심화/개선 여부를 반영한다.
  - Dependencies: `buildPrompt()` 개선 완료
  - 예상 소요: 1h

---

## Epic 7: 서버 인프라 개선 및 배포

- 설명: 현재 Oracle Cloud e.1(1GB RAM) 무료 티어에서 a1(Ampere ARM, 4코어/24GB RAM) 인스턴스로 마이그레이션하고, Chrome 웹 스토어 비공개 배포를 완료한다.

---

### Story 7-1: Oracle Cloud a1 인스턴스에서 서버를 안정적으로 운영할 수 있다

- 설명: 실험 3주 기간 동안 서버 다운 없이 안정적으로 운영하기 위해 e.1에서 a1.flex 인스턴스로 마이그레이션하고, 기존 DB 데이터를 이전하고 CI/CD 파이프라인을 재연결한다.
- Priority: High
- Owner: BE
- Definition of Done: a1 인스턴스의 `/health` 엔드포인트가 외부에서 200 응답을 반환하고, 기존 DB 데이터가 정상 이전된다.
- Dependencies: Story 4-3 완료

#### Tasks

- [ ] Oracle Cloud a1.flex 인스턴스 생성 및 환경 구성
  - Priority: High / Owner: BE
  - 내용: Ampere ARM a1.flex 인스턴스 생성. Node.js 설치, 방화벽 인바운드 규칙(포트 3000 또는 80/443) 설정, pnpm 설치
  - DoD: 인스턴스에 SSH 접속 가능하고 Node.js + pnpm이 동작한다.
  - Dependencies: Oracle Cloud 계정
  - 예상 소요: 2~3h

- [ ] 기존 e.1 인스턴스 DB 마이그레이션
  - Priority: High / Owner: BE
  - 내용: e.1 인스턴스의 `youtube_bias.db` 파일을 SCP로 a1 인스턴스로 복사. 마이그레이션 후 테이블 row 수 비교로 정합성 확인
  - DoD: a1 인스턴스 DB에 기존 세션/참여자/영상이벤트 데이터가 동일하게 존재한다.
  - Dependencies: a1 인스턴스 환경 구성 완료
  - 예상 소요: 1~2h

- [ ] GitHub Actions CI/CD 파이프라인 타겟 서버 변경
  - Priority: Medium / Owner: BE
  - 내용: `.github/workflows/` 배포 스크립트의 SSH 호스트 및 IP를 a1 인스턴스로 변경. secrets 업데이트 후 자동 배포 테스트
  - DoD: `main` 브랜치 push 시 a1 인스턴스에 자동 배포되고 `/health`가 정상 응답한다.
  - Dependencies: a1 인스턴스 환경 구성 완료
  - 예상 소요: 1~2h

- [ ] `config.js` SERVER_URL을 a1 인스턴스 URL로 업데이트
  - Priority: High / Owner: FE
  - 내용: `extension/config.js`의 `SERVER_URL`을 새 a1 서버 URL로 변경. `config.example.js`도 동기화
  - DoD: 확장 프로그램이 새 서버 URL로 세션 데이터를 정상 전송한다.
  - Dependencies: a1 인스턴스 배포 완료
  - 예상 소요: 30m

---

### Story 7-2: Chrome 웹 스토어를 통해 참여자가 확장 프로그램을 설치할 수 있다

- 설명: Chrome 웹 스토어에 비공개(Unlisted) 확장 프로그램으로 등록하여 링크를 가진 참여자만 설치 가능하도록 한다. 심사 지연 대비 zip 직접 배포 방식을 병행 준비한다.
- Priority: High
- Owner: FE
- Definition of Done: 웹 스토어 링크 또는 zip 파일로 참여자가 확장 프로그램을 설치하고 온보딩을 완료할 수 있다.
- Dependencies: Story 7-1 완료

#### Tasks

- [ ] 웹 스토어 제출용 배포 빌드 패키징
  - Priority: High / Owner: FE
  - 내용: a1 서버 URL이 반영된 `config.js`로 최종 빌드. 미사용 파일(`popup.js`, `ViewLens.html` 등) 제외하여 zip 패키징. `manifest.json` 버전 넘버 확인
  - DoD: 빌드된 zip을 Chrome 개발자 모드로 수동 설치 시 정상 동작한다.
  - Dependencies: `config.js` SERVER_URL 업데이트 완료
  - 예상 소요: 1~2h

- [ ] Chrome 웹 스토어 비공개 확장 프로그램 제출
  - Priority: High / Owner: FE
  - 내용: Chrome Developer Dashboard에서 배포 가시성을 Unlisted로 설정하여 제출. 스토어 설명(한국어), 스크린샷 2~4장, 개인정보 처리 방침 URL 작성
  - DoD: 웹 스토어 심사 제출이 완료되고 심사 대기 상태가 확인된다.
  - Dependencies: 배포 빌드 패키징 완료
  - 예상 소요: 2~3h

- [ ] 참여자용 zip 직접 설치 가이드 작성
  - Priority: Medium / Owner: FE
  - 내용: 심사 지연 대비 GitHub Releases에 zip 업로드. 비개발자도 따라할 수 있는 단계별 설치 가이드(Chrome 개발자 모드 활성화 → zip 압축 해제 → 로드) 작성
  - DoD: 가이드만 보고 비개발자가 확장 프로그램을 설치할 수 있다.
  - Dependencies: 배포 빌드 패키징 완료
  - 예상 소요: 1~2h

---

## Epic 8: 실험 운영 안정화

- 설명: 본격적인 실험 시작 전 주요 이슈를 수정하고, 사전 테스팅을 통해 안정성을 확보한다. 실험 종료 후 대조군에게 결과를 제공하는 기능도 구현한다.

---

### Story 8-1: anonymousId가 서버 participants 테이블에 정상 반영된다

- 설명: 온보딩에서 생성된 UUID 기반 anonymousId가 `POST /api/participants`를 통해 서버 DB에 정확히 저장되는지 검증하고, 동기화 문제가 있을 경우 수정한다.
- Priority: High
- Owner: FE / BE
- Definition of Done: 신규 설치 → 코드 입력 → 온보딩 완료 플로우 수행 시 서버 `participants` 테이블에 해당 `anonymousId`와 `group_code`가 저장된다.
- Dependencies: Story 7-1 완료 (a1 서버 배포)

#### Tasks

- [ ] 온보딩 완료 플로우 엔드투엔드 테스트
  - Priority: High / Owner: FE
  - 내용: 확장 프로그램 재설치 → 코드 입력 → 온보딩 완료 후 서버 로그 및 DB `participants` 테이블 확인. `POST /api/participants` 요청 발생 여부, 요청 body 정확성 검증
  - DoD: 온보딩 완료 시 participants 테이블에 레코드가 생성된다.
  - Dependencies: a1 서버 배포 완료
  - 예상 소요: 1h

- [ ] `storage.set` 완료 후 서버 요청 전송 순서 보장 (이슈 확인 시)
  - Priority: High / Owner: FE
  - 내용: `viewlens-popup.js` `onChange` 콜백에서 `chrome.storage.local.set()` 완료 이후에 `fetch('/api/participants')`가 실행되도록 `await` 순서 검토 및 수정
  - DoD: 서버 전송 시점에 `anonymousId`가 storage에 확실히 저장되어 있고, 서버와 로컬 storage의 anonymousId 값이 일치한다.
  - Dependencies: 엔드투엔드 테스트 완료 (이슈 재현 시)
  - 예상 소요: 1~2h

---

### Story 8-2: 사전 테스팅을 통해 배포 전 주요 버그를 해결할 수 있다

- 설명: 교수님 및 내부 테스터 3~5명 대상으로 사전 테스트를 진행하고 피드백을 반영하여 본 실험 전 안정성을 확보한다.
- Priority: High
- Owner: FE / BE
- Definition of Done: 온보딩, 영상 감지, 세션 분석, 피드백 생성, 서버 전송의 5개 핵심 플로우에 크리티컬 버그 0건.
- Dependencies: Story 7-2 완료 (zip 배포 준비)

#### Tasks

- [ ] 테스트 시나리오 체크리스트 작성
  - Priority: High / Owner: FE
  - 내용: (1) 온보딩 코드 입력 및 그룹 배정, (2) YouTube 영상 시청 후 세션 감지 확인, (3) 10분 비활성 후 피드백 자동 생성, (4) 팝업 오늘/주차별 탭 확인, (5) 서버 전송 여부 확인 시나리오 작성
  - DoD: 테스터가 체크리스트만으로 독립적으로 테스트를 수행할 수 있다.
  - Dependencies: 없음
  - 예상 소요: 1~2h

- [ ] 교수님 및 내부 테스터 배포 및 테스트 진행
  - Priority: High / Owner: FE
  - 내용: zip 파일 + 설치 가이드 배포. EXP/CON 각 그룹 코드 전달. 테스트 기간: 2026.06.15 ~ 2026.06.30
  - DoD: 테스터로부터 버그 리포트 또는 "이상 없음" 확인이 수집된다.
  - Dependencies: 테스트 시나리오 완료, zip 배포 준비 완료
  - 예상 소요: 진행 기간 2주 (2026.06.15 ~ 2026.06.30)

- [ ] 테스트 피드백 기반 버그 수정
  - Priority: High / Owner: FE / BE
  - 내용: 수집된 버그를 우선순위별로 수정. 우선순위: (1) 데이터 수집 누락, (2) 서버 전송 실패, (3) UI 오류, (4) 피드백 품질
  - DoD: 크리티컬 버그 전량 해결 후 재테스트 통과.
  - Dependencies: 테스터 피드백 수집 완료
  - 예상 소요: 2026.06.15 ~ 2026.06.30 중 이슈 발생 시

---

### Story 8-3: 실험 종료 후 대조군 참여자에게 분석 결과를 제공할 수 있다

- 설명: 3주 실험 종료 후 CON 그룹 참여자에게도 누적 시청 기록 분석 결과를 일괄 제공하여 연구 윤리 요건을 충족한다.
- Priority: Low
- Owner: FE / BE
- Definition of Done: 실험 종료(설치 후 21일) 이후 CON 그룹 팝업에서 3주간 누적 분석 결과를 확인할 수 있다.
- Dependencies: 3주 실험 완료

#### Tasks

- [ ] CON 그룹 실험 종료 후 결과 공개 분기 처리
  - Priority: Low / Owner: FE
  - 내용: `viewlens-app.js`의 `screenControlHome()` 렌더링 분기에서 실험 종료일(설치 후 D+21) 이후 CON 그룹에게 EXP와 동일한 주차별 피드백 탭을 노출하도록 수정
  - DoD: CON 그룹 참여자가 D+21 이후 팝업에서 주차별 분석 리포트를 확인할 수 있다.
  - Dependencies: 없음
  - 예상 소요: 2~3h

- [ ] CON 그룹 결과 공개 화면 안내 문구 작성
  - Priority: Low / Owner: FE
  - 내용: 결과 공개 시 "실험이 종료되었습니다. 3주간의 시청 분석 결과를 확인해보세요." 안내 문구 및 전환 UX 구현
  - DoD: CON 그룹 결과 공개 시점에 자연스러운 전환 UX가 제공된다.
  - Dependencies: CON 그룹 분기 처리 완료
  - 예상 소요: 1h

---

## Epic 9: 기술 부채 정리

- 설명: 새 팝업 구조(`viewlens-*.js`)로 전환되면서 사용하지 않게 된 구 코드를 정리하여 빌드 패키지 크기를 줄이고 코드베이스 혼란을 방지한다.

---

### Story 9-1: 미사용 구 팝업 코드를 제거하여 코드베이스를 정리할 수 있다

- 설명: `popup.html`에서 로드하지 않는 구 구현체 `popup.js`를 제거한다. 배포 zip 패키지에 미사용 파일이 포함되지 않도록 한다.
- Priority: Low
- Owner: FE
- Definition of Done: `popup.js` 제거 후 팝업이 정상 동작하고 웹 스토어 제출 zip에 포함되지 않는다.
- Dependencies: Story 7-2 배포 빌드 패키징 작업 전

#### Tasks

- [ ] `popup.js` 미사용 여부 최종 확인 및 제거
  - Priority: Low / Owner: FE
  - 내용: `popup.html`, `manifest.json`, 모든 스크립트에서 `popup.js` 참조 여부 확인 후 파일 삭제
  - DoD: `popup.js` 삭제 후 팝업 열기, 온보딩, 세션 데이터 표시가 정상 동작한다.
  - Dependencies: 없음
  - 예상 소요: 30m

- [ ] 웹 스토어 배포 zip에서 불필요한 파일 제외 목록 정의
  - Priority: Low / Owner: FE
  - 내용: `ViewLens.html`(디자인 스튜디오 전용), `tweaks-panel.*`(디자인 스튜디오 전용), `config.example.js` 등 배포 시 포함할 필요 없는 파일 목록 정의. `.zipignore` 또는 패키징 스크립트로 관리
  - DoD: 배포 zip에 개발 전용 파일이 포함되지 않는다.
  - Dependencies: 없음
  - 예상 소요: 1h
