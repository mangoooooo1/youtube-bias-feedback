# 작업 구조 (3차)

6주 확대 실험을 대비하여, 개인정보 체계(가명처리)·상호작용 로깅·성능 로깅·데이터 저장소를 보강한다.

---

## Epic 10: 데이터·개인정보 보강

- 설명: 실험 규모·기간 확대(참여자 80명 목표, 총 6주)에 필요한 데이터·개인정보 요건을 충족하도록 시스템을 보강한다.
- 작업 순서(효율): 10-1(개인정보·IRB) → 10-2(DB·스키마) → 10-3(latency) → 10-4(폴백) → 10-5(마이크로 로그)

---

### Story 10-1: 가명처리 기반 식별자 체계 및 IRB / 개인정보 처리방침 재설계

- 설명: 신원 정보(이름·연락처)와 행동 데이터를 **분리 보관**하고, 연구자가 배정한 고유 코드로 **가명처리**한다. 인터뷰 연결은 연구자가 보관하는 별도 연결 명단으로 수행하며, 개인정보 처리방침·IRB 신청서에 반영한다.
- Priority: High
- Owner: FE / Research
- Definition of Done: 서버에는 이름·연락처 등 직접 식별정보가 저장되지 않고 코드/UUID만 저장된다. 개인정보 처리방침이 "가명처리(분리 보관)"로 개정되고, IRB 신청서에 식별자·보관·파기 계획이 반영된다.
- Dependencies: 없음 (개인정보·IRB 선행 결정 — 다른 이슈의 스키마가 이 결정 위에 구현됨)

#### 설계 결정 (배경)

- **익명 vs 가명**: 모집 시 이름·연락처를 받으므로 "완전 익명"이 아니라 **가명처리(pseudonymization)** 이다. 핵심은 신원 데이터와 행동 데이터의 **분리 보관(unlinkability)**.
- **데이터 분리 구조 (3분할)**
  1. **신원·연락 명단** (이름/전화/이메일): 동의·보상·인터뷰 연락 목적. **연구자만 별도 안전 보관**(오프라인/암호화). 서버·행동 데이터와 절대 결합하지 않음.
  2. **행동 데이터** (서버): 시청·분석 데이터를 **코드(또는 UUID)로만** 저장. 직접 식별정보 없음.
  3. **연결 명단** (코드 ↔ 신원): 별도 보관. **연구·인터뷰 종료 후 파기** → 잔여 데이터는 사실상 익명화.
- **식별자 사용성**: 36자 UUID를 참여자가 직접 가져오게 하는 방식은 비실용적 → **연구자 배정 고유 코드**(그룹 접두사 + 짧은 랜덤, 예: `EXP-3F9K` / `CON-7B2M`)를 채택.
- **인터뷰 연결 방식**: 6주차 배너로 안내 후, 참여자는 **짧은 자기 코드만 제시/연락** → 연구자가 연결 명단에서 조회하여 해당 코드의 6주 다양성 그래프를 확인. (참여자가 UUID를 찾아올 필요 없음)
- **참여자 설명 논거**: 시청 기록은 정치 성향·관심사 등 민감정보를 드러낼 수 있어, 신원과 분리해 저장한다. 그래서 데이터가 유출되더라도 "누가 무엇을 봤는지"를 특정할 수 없다는 점이 참여자에게 주는 실질적 보호다.

#### Tasks

- [ ] 참여자별 고유 코드 체계 설계 (그룹 접두사 + 짧은 랜덤, EXP/CON 배정 포함)
- [ ] 온보딩을 단일 그룹 코드 입력 → 고유 참여 코드 입력으로 확장 (그룹은 코드에서 파생)
- [ ] 서버 저장 데이터에 이름·연락처 미포함 확인 (코드/UUID만 저장)
- [ ] 신원 ↔ 코드 연결 명단의 별도 보관·암호화·파기 운영 방침 수립
- [ ] 6주차 인터뷰 모집 배너 + 연구자 명단 조회 방식 정의 (참여자는 짧은 코드 제시)
- [ ] 개인정보 처리방침 개정: "완전 익명" → "가명처리(분리 보관)", 연구 종료 후 연결 명단 파기 명시
- [ ] IRB 신청서에 식별자·동의·보관/파기 계획 반영

- 관련 문서: `PRIVACY.md`

---

### Story 10-2: 데이터 저장소(SQLite) 유지·이전 검토 및 스키마 확정

- 설명: 마이크로 로그·latency·폴백 로그 등 수집 항목 증가에 대비해 SQLite 유지 여부를 검토하고 신규 스키마를 확정한다.
- Priority: High / Owner: BE
- 핵심 고려: 수십~백 명·단일 서버·append 위주면 SQLite로 충분할 수 있음(성급한 이전 경계). 유지 시 WAL 모드·백업 정책 설정. **이 스키마 확정 후 10-3~10-5를 구현**해야 재작업이 없다.

### Story 10-3: 세션 처리 지연시간(Latency) 로깅

- 설명: 세션 종료 후 YouTube API 호출 ~ Gemini 피드백 생성까지 처리 시간(ms)을 측정·저장한다. (실시간 시스템 성능 지표)
- Priority: Medium / Owner: BE
- 핵심 고려: total 및 단계별(youtubeMs/geminiMs) 분리 저장. 실패(타임아웃) 소요 시간도 구분 기록.

### Story 10-4: LLM 실패·폴백 작동 로깅

- 설명: Gemini 호출 실패로 규칙 기반 폴백이 작동한 경우와 사유를 기록한다. (시스템 견고성 지표)
- Priority: Medium / Owner: BE
- 핵심 고려: 세션별 "LLM 성공/폴백" 플래그 + 실패 사유 분류. 10-3과 같은 경로에 담으면 효율적. 개인정보 아님.

### Story 10-5: 팝업 상호작용(체류시간·탭 클릭) 마이크로 로그

- 설명: 팝업 체류 시간(초)과 탭(오늘/주차별) 클릭 여부·횟수를 수집·전송한다. (사용자 상호작용/행동 분석용)
- Priority: Medium / Owner: FE
- 핵심 고려: 팝업 close 시 전송(visibilitychange/beforeunload) 방식, 전송 실패 대비 버퍼/재시도, baseline 기간 수집 여부를 실험 설계와 일치시킬 것. 스키마는 10-2 확정 후.
