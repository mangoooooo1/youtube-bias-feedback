# 작업 구조 (4차)

`docs/05-작업3.md`(Epic 10) 진행 중 발견된 후속 작업을 다룬다. 완료된 일/주차 돌아보기 탭의 리뷰 생성 방식과 "오늘" 탭의 리뷰 집계 기준은 둘 다 "참여자에게 어떤 근거로 만들어진 AI 리뷰 텍스트를 보여줄 것인가"라는 같은 질문에서 나왔지만, Epic 10의 원래 정의("데이터·개인정보 보강")와는 성격이 달라 **Epic 11로 분리**한다.

---

## Epic 11: 피드백 리뷰 생성·노출 전략

- 설명: 참여자에게 노출되는 AI 리뷰 텍스트를 "언제, 어떤 범위의 데이터를 근거로, 누구에게" 보여줄지에 대한 로직을 다룬다. 세션 단위 리뷰 생성(`llm.js`, Epic 10 Story 10-11에서 이미 보강됨) 자체는 이 Epic의 대상이 아니며, 그 위에서 기간·화면 단위로 리뷰를 어떻게 재구성·노출할지가 범위다.
- 진행 현황: 11-1·11-2 모두 **구현 완료**(클라이언트·서버 코드 반영, vitest·eslint 통과). 남은 건 파일럿·운영 검증 항목뿐 — 각 Story의 Tasks "공통" 절(미체크 항목) 참고.
- Epic 10 Story 10-10("연구 종료 시점 참여자 안내 + 대조군 일괄 피드백 공개")은 이 Epic과 밀접하게 관련되지만, 이미 `작업3`에서 Epic 10 소속으로 여러 차례 인용되고 있어(진행 현황·작업 순서·Dependencies) 지금 옮기면 기존 문서의 참조가 깨진다. **Epic 10에 그대로 두고 이 문서에서는 교차 참조만 한다** — 아래 "Story 10-10과의 관계" 참고.

---

### Story 11-1: 기간(일차·주차) 리뷰 생성

- 설명: 완료된 일차/주차 돌아보기 탭에 그 기간 전체를 요약하는 전용 AI 리뷰를 서버 배치(cron)로 생성해 저장한다. 지금은 그 기간 마지막 세션의 리뷰를 그대로 재사용하고 있어, 탭 라벨("이 기간을 돌아본다")과 실제 표시 내용(가장 최근 세션 하나)이 어긋나 있다.
- Priority: High / Owner: BE + FE
- Dependencies: 없음 — 세션 리뷰(`llm.js`)와 독립적으로 동작
- 상태: **구현 완료**(아래 Tasks 전부 체크). 상세 명세서는 `docs/blog/기간-리뷰-생성-명세서.md`에 있으며 gitignore 대상이라 링크 생략(로컬에서만 열람 가능)

#### 설계 결정 (배경)

- **생성 위치 — 서버 배치(cron)**: 클라이언트 온디맨드/알람 트리거 방식도 검토했으나, (1) 연구 재현성을 위해 결과를 서버에 저장해야 하는 요구사항이 이미 있고, (2) 참여자가 며칠간 접속하지 않아도 서버는 항상 켜져 있어 "밀린 기간" 문제 자체가 사라지며, (3) 프롬프트를 익스텐션 재배포 없이 즉시 튜닝할 수 있다는 점에서 서버 배치가 우위로 판단됨.
- **영상 제목 활용 가능**: 처음엔 "서버는 원시 데이터(영상 제목)가 없어 클라이언트보다 피드백이 추상적일 것"으로 판단했으나, 이미 `video_events` 테이블에 영상 제목이 세션과 무관하게 저장되고 있음을 확인(`extension/content.js` → `POST /api/video-events`) — 서버도 세션 리뷰 수준의 구체적인 피드백을 만들 수 있다.
- **API 키 분리**: Gemini 요청 한도는 API 키가 아니라 Google Cloud 프로젝트 단위로 걸리므로, 기간 리뷰(배치, 버스트 위험)와 세션 리뷰(실시간, 참여자 체감 경험)를 별도 GCP 프로젝트로 분리해 한쪽 장애가 다른 쪽까지 끌고 가지 않게 한다.
- **적용 범위**: 완료된 기간에만 적용, 실험군(EXP)만 대상. 진행 중인 "오늘" 탭과 대조군 공개는 각각 Story 11-2·10-10에서 별도로 다룬다.
- 상세 설계 결정·근거는 명세서의 각 절(특히 11·12·15·18절 "근거" 박스) 참고.

#### Tasks

- [x] `server/db.js` — `period_reviews` 테이블 추가
- [x] `server/routes/period-reviews.js` — `GET /api/period-reviews` 엔드포인트
- [x] `server/app.js` — 라우트 등록
- [x] `server/pipeline/period-review-llm.js` — 기간용 프롬프트 빌더·가드레일·fallback (Node.js 이식)
- [x] `server/scripts/generate-period-reviews.js` — cron 진입점
- [x] 서버 crontab에 매일 04:00 KST 등록 (기존 백업 23:00·08:00 KST와 안 겹침)
- [x] `extension/popup/viewlens-popup.js` — `buildWeeksData`의 review 출처를 서버 fetch 결과로 교체 + 로컬 캐싱
- [x] 기간 리뷰용 Gemini API 키를 별도 GCP 프로젝트에서 발급, `PERIOD_REVIEW_GEMINI_API_KEY`로 서버 `.env`에 등록
- [x] IRB/개인정보 처리방침에 "서버가 수집한 영상 제목을 LLM(Gemini) 처리에 사용한다"는 흐름이 승인 범위에 포함되는지 확인 (Story 10-11의 선례 — `review`/`reviewTopic` 저장도 경미 변경 문의가 필요했음)
- [x] 파일럿 검증용 `DAYS_PER_PERIOD` 1→2, 총 6일(베이스라인 2일+일반 2일×2) 재구성
- [x] 서버 vitest 도입(레포 최초) — 프롬프트/가드레일/fallback 유닛 테스트

- 관련 문서: 상세 명세서는 `docs/blog/기간-리뷰-생성-명세서.md`에 있으며 gitignore 대상이라 링크 생략(로컬에서만 열람 가능)

---

### Story 11-2: "오늘" 탭 누적 리뷰 생성

- 설명: "오늘" 탭은 카테고리 분포·도넛차트·`videoCount`·"분석한 영상" 목록은 오늘 세션 전체를 합친 누적 데이터를 보여주는데, 정작 리뷰 문단(`vlReview`)만 `sourceSessions.at(-1)?.review`로 **가장 최근 세션 하나**의 텍스트를 보여준다. 예를 들어 아침에 게임 영상을 보고 방금 뉴스 영상을 봤다면, 도넛차트엔 게임+뉴스가 다 나오는데 리뷰 문단은 방금 본 뉴스 얘기만 하게 된다. 기존 세션 리뷰 카드(블러/확인)는 그대로 두고, 그 위에 **오늘 하루 전체를 반영하는 별도의 "누적 리뷰" 카드**를 추가한다.
- Priority: Medium / Owner: FE 중심 + BE(저장)
- Dependencies: 없음 — Story 11-1과 독립. 세션 리뷰 파이프라인(`extension/pipeline/llm.js`)을 클라이언트 백그라운드에서 그대로 재사용한다.
- 상태: **구현 완료** — 남은 건 아래 Tasks "공통" 절의 파일럿 확정 항목뿐. 명세서는 `docs/blog/`에 있으며 gitignore 대상이라 링크 생략

#### 설계 결정 (배경)

- **방향 — 세션 카드 유지 + 누적 카드 추가(2카드)**: 세션 리뷰 카드의 블러/확인은 연구 측정 신호 두 개(`feedbackConfirmedAt` 엄격 / `feedbackViewed` 패시브)를 남기는 장치다. 이를 보존하기 위해 세션 카드는 그대로 두고, 블러 없는 누적 리뷰 카드를 별도로 얹는다. 누적 카드는 측정과 무관한 순수 노출 개선.
- **생성 위치 — 클라이언트 백그라운드 워커**: 팝업은 classic script라 ESM `llm.js`를 import할 수 없다(`viewlens-popup.js:545-546`에 명시된 경계). 이미 `llm.js`를 쓰는 `background.js`에서 생성하고 팝업은 `chrome.runtime.sendMessage`로 요청한다. 기존 `llm.js`를 그대로 재사용 — 11-1과 달리 서버 이식이 불필요(브라우저 실행 환경 동일).
- **트리거 — 팝업 오픈 시 지연 생성(lazy) + 하루 상한 N**: 팝업이 열릴 때, 지난 생성 이후 새 세션이 있고 하루 상한 N 미만일 때만 백그라운드에서 1회 생성. 안 여는 참여자껀 호출 0회. "매 세션 종료 시 생성"(세션당 Gemini 호출 2배)을 피하기 위한 선택. 생성 중엔 "분석 중…" 플레이스홀더(블러 아님).
- **저장 — 서버 1일 1행 upsert**: `today_reviews`에 `(anonymousId, reviewDate)` UNIQUE로 그날의 **최신 누적 스냅샷 1건만** 덮어써 저장한다(저장 부담 최소). 하루 중 여러 번 재생성돼도 이전 버전(중간 리뷰 텍스트·집계값·`sourceSessionIds`)은 서버에 남지 않는다 — 여기서 "재현 가능"은 참여자가 최종적으로 노출된 오늘 개입 내용을 추적할 수 있다는 뜻이며, 하루 중 세션 단위 변화 이력까지 복원 가능하다는 의미는 아니다(이력 미보관은 명세서 §19에서 범위 밖으로 명시적으로 확정됨). 서버는 저장만 담당(이 기능에서 Gemini 미호출). API 키도 세션 리뷰용 클라이언트 키를 재사용(별도 GCP 프로젝트 불필요).
- **적용 범위**: 진행 중인 "오늘"만, 실험군(EXP)·비베이스라인·오늘 세션 있음일 때만. 완료된 날짜는 Story 11-1 기간 리뷰가 담당.
- 관련 코드: `extension/popup/viewlens-popup.js`(`buildDataForDate`), `extension/popup/viewlens-screens.js`(`screenToday`), `extension/background.js`(세션 리뷰 생성), `extension/pipeline/llm.js`

#### Tasks

**클라이언트**

- [x] `extension/pipeline/llm.js` — `buildTodayCumulativePrompt`(오늘 전체 프레이밍) + `TODAY_PROMPT_VERSION` 추가 (`generateReview`·`generateFallbackReview`·`SENSITIVE_PATTERN` 재사용)
- [x] `extension/background.js` — `chrome.runtime.onMessage`에 "오늘 누적 생성" 케이스 추가 (오늘 세션 병합 집계 → 생성 → 캐시 저장 → 생성 횟수 +1 → 서버 upsert)
- [x] `extension/popup/viewlens-popup.js` — 누적 캐시 조회·신선도 판정·상한 확인·생성 요청, `VL._todayCumulative`에 누적 상태 보관(`VL.today`는 날짜 이동마다 재생성돼 담을 수 없어 `VL._todayConfirmed`와 같은 방식으로 분리), `storage.onChanged` 반영
- [x] `extension/popup/viewlens-screens.js` — `screenToday`에 블러 없는 누적 리뷰 카드 렌더 추가

**서버**

- [x] `server/db.js` — `today_reviews` 테이블 추가
- [x] `server/routes/today-reviews.js` — `POST /api/today-reviews` (upsert)
- [x] `server/app.js` — 라우트 등록

**공통**

- [ ] 하루 상한 N 확정(초안 6, `viewlens-popup.js`의 `TODAY_CUMULATIVE_DAILY_CAP`), 두 카드의 제목·순서·"분석한 영상" 목록 중복 여부를 파일럿에서 확정
- [x] IRB/개인정보 처리방침에 `today_reviews` 리뷰 텍스트 서버 저장이 승인 범위인지 확인 (Story 10-11 선례 — `sessions.review` 저장과 동일 성격 → 승인 범위 확인됨)
- [ ] 세션 리뷰 블러/확인 흐름과 `feedbackConfirmedAt`·`feedbackViewed` 로깅이 회귀 없이 유지되는지 확인 (구현상 해당 경로는 변경하지 않았으나, 실제 브라우저에서 최종 확인 필요)

- 구현 중 명세서 범위를 벗어나 추가로 발견·수정한 사항:
  - `generateReview`가 `promptVersion`을 세션 버전으로 하드코딩 반환하던 결함 수정(파라미터화) — 오늘 누적 리뷰가 LLM 성공 경로로 생성돼도 `today_reviews.promptVersion`에 세션 버전이 잘못 찍히는 문제였음
  - 세션 전용 폴백 문구("이번 세션에서는…")가 누적 카드에도 그대로 노출되는 문제 발견 → `generateTodayFallbackReview` 신설로 해결
  - `vlReview` 컴포넌트에 `id` 파라미터 추가 — 세션 카드와 누적 카드가 한 화면에 동시에 렌더될 때 DOM id(`vl-review-card`) 충돌 방지(기본값은 기존과 동일해 하위 호환)

---

### Story 10-10과의 관계 (Epic 10 소속, 이 문서로 옮기지 않음)

`작업3`(Epic 10)에 이미 정의된 Story 10-10("연구 종료 시점 참여자 안내 + 대조군 일괄 피드백 공개")은 아직 미착수다. 번호와 소속 Epic은 그대로 두되, Story 11-1 착수 전에는 없었던 선택지가 하나 새로 생겨서 여기에 기록해둔다.

- **기존 제안**(`docs/blog/피드백.md` §4): 대조군 전용 `buildSummaryPrompt(categoryDistribution, baselineEntropy, finalEntropy, ...)`를 클라이언트(`llm.js`)에 추가해, 연구 종료 시점에 6주 누적을 "지난 6주간"으로 프레이밍한 **단발성 요약 1건**을 생성.
- **11-1 이후 새로 생긴 선택지**: `period_reviews`가 이미 매 기간(주차)마다 쌓여 있다면, 연구 종료 시 대조군에게 그 기간들을 그대로 "언락"해서 실험군이 매주 봤던 것과 같은 형태(주차별 탭)로 보여줄 수도 있다. Story 10-10의 Task 목록에 있던 "기존 주차별 탭 재사용 vs 신규 화면" 갈림길이 바로 이 지점과 맞닿아 있다.
- **주의할 차이**: `period_reviews`를 대조군에 그대로 쓰려면 대조군도 매 기간 리뷰가 생성되고 있어야 하는데, Story 11-1은 지금 EXP 전용으로 설계돼 있다. 대조군까지 대상에 포함해서 조용히 미리 생성해둘지(참여자에겐 비공개 상태로 쌓아만 두기), 아니면 연구 종료 시점에 그때 한꺼번에 소급 생성할지는 결정이 필요하다.
- 이 부분은 Story 10-10 자체를 다시 논의할 때(10-9 설문 문항 확정 이후 착수 예정) 함께 정리한다. 지금은 미결정 상태로만 남겨둔다.
