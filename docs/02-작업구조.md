# 작업 구조

---

## Epic 1: 크롬 확장 프로그램 - 핵심 데이터 수집

- 설명: YouTube 시청 중인 영상의 ID와 제목을 실시간으로 감지하고 세션 단위로 로컬에 저장한다.

---

### Story 1-1: YouTube 재생 영상 ID와 제목을 자동으로 감지할 수 있다

- 설명: 사용자가 YouTube에서 영상을 시청하면 확장 프로그램이 현재 재생 중인 영상의 ID와 제목을 자동으로 감지한다.
- Priority: High
- Owner: FE
- Definition of Done: YouTube 영상 페이지 진입 및 SPA 이동 시 videoId와 제목이 콘솔/storage에 정확히 기록된다.
- Dependencies: 없음

#### Tasks

- [x] manifest.json 기본 구조 작성
  - Priority: High / Owner: FE
  - 내용: Manifest V3 기준으로 작성. `permissions`: `storage`, `activeTab` / `host_permissions`: `*://*.youtube.com/*` / content script, background service worker 선언
  - DoD: Chrome 확장 프로그램이 오류 없이 로드된다.
  - Dependencies: 없음
  - 예상 소요: 1~2h

- [x] content script, background service worker 파일 뼈대 생성
  - Priority: High / Owner: FE
  - 내용: `content.js`, `background.js` 파일 생성 및 message passing 구조 설계
  - DoD: content → background로 메시지 전송이 콘솔에서 확인된다.
  - Dependencies: manifest.json 작성 완료
  - 예상 소요: 1~2h

- [x] YouTube URL에서 videoId 파라미터 추출 로직 구현
  - Priority: High / Owner: FE
  - 내용: `location.href`에서 `v=` 쿼리 파라미터 추출. `/shorts/` 경로도 처리
  - DoD: 일반 영상, Shorts 모두 videoId가 정확히 추출된다.
  - Dependencies: content script 뼈대 완료
  - 예상 소요: 1~2h

- [x] MutationObserver로 YouTube SPA 페이지 이동 감지 구현
  - Priority: High / Owner: FE
  - 내용: YouTube는 SPA이므로 URL 변경 시 `popstate`, `yt-navigate-finish` 이벤트 또는 `MutationObserver`로 페이지 전환 감지
  - DoD: 다른 영상으로 이동 시 새 videoId 감지가 콘솔에 출력된다.
  - Dependencies: videoId 추출 로직 완료
  - 예상 소요: 2~3h

- [x] 영상 제목 DOM에서 파싱하는 로직 구현
  - Priority: Medium / Owner: FE
  - 내용: `document.title` 또는 `h1.ytd-video-primary-info-renderer` 셀렉터로 제목 추출. 제목 로드 지연 대응(polling 또는 MutationObserver)
  - DoD: 영상 진입 후 2초 내 제목이 정확히 추출된다.
  - Dependencies: MutationObserver 감지 완료
  - 예상 소요: 1~2h

---

### Story 1-2: 감지된 영상 데이터를 세션 단위로 로컬에 저장할 수 있다

- 감지된 videoId, 제목, 시청 시각을 `chrome.storage.local`에 세션 단위로 저장한다.
- Priority: High
- Owner: FE
- Definition of Done: 시청 후 `chrome.storage.local`을 조회하면 현재 세션에 영상 항목이 누락 없이 쌓여 있다.
- Dependencies: Story 1-1 완료

#### Tasks

- [x] chrome.storage.local 저장 데이터 구조 설계 및 구현
  - Priority: High / Owner: FE
  - 내용: `{ currentSession: { sessionId, startTime, videos: [{videoId, title, watchedAt}] }, sessions: [...] }` 형태 설계
  - DoD: 설계 문서 또는 주석으로 구조가 명세되고, 저장/조회 함수가 동작한다.
  - Dependencies: Story 1-1 완료
  - 예상 소요: 2~3h

- [x] 중복 videoId 방지 로직 구현
  - Priority: Medium / Owner: FE
  - 내용: 같은 세션 내에서 동일 videoId가 연속 감지될 때 중복 저장하지 않는 로직
  - DoD: 새로고침이나 반복 감지 시 동일 항목이 중복 저장되지 않는다.
  - Dependencies: 저장 구조 구현 완료
  - 예상 소요: 1~2h
  - 검토 필요: `content.js`의 인메모리 `lastVideoId`는 비디오 페이지 → 비디오 없는 페이지(홈 등) → 동일 비디오 재진입 시 재감지가 차단되는 사이드 이펙트가 있음. storage 기반 중복 방지 로직 설계 시 `lastVideoId`를 null로 리셋하는 시점도 함께 결정할 것 (관련 PR 코멘트: feat/55-extract-youtube-video-metadata)
  - 반영 결과: storage 기반 대신 인메모리 리셋 방식(Option A) 채택. `extractVideoId()`가 `null`을 반환하는 시점(비디오 없는 페이지 진입 시) `lastVideoId = null`로 리셋하도록 `content.js` 수정. 영상A → 홈 → 영상A 재진입 시 정상 재감지되며, 새로고침 및 연속 중복은 기존 `lastVideoId` 비교로 방지.

- [x] 저장/조회/초기화 유틸 함수 모듈화
  - Priority: Low / Owner: FE
  - 내용: `storage.js` 파일로 분리. `addVideo()`, `getCurrentSession()`, `getAllSessions()`, `clearSession()` 함수 구현
  - DoD: 각 함수가 독립적으로 호출 가능하고 예상 결과를 반환한다.
  - Dependencies: 저장 구조 구현 완료
  - 예상 소요: 1~2h

---

## Epic 2: 분석 파이프라인

- 설명: 수집된 영상 데이터를 기반으로 카테고리 분류, 편향 수치화, 자연어 리뷰 생성까지의 분석 흐름을 구현한다.

---

### Story 2-1: 30분 비활성 기준으로 세션 종료를 자동으로 감지할 수 있다

- 설명: 마지막 영상 시청 후 30분이 경과하면 현재 세션을 종료 처리하고 분석 파이프라인을 트리거한다.
- Priority: High
- Owner: FE
- Definition of Done: 마지막 시청 후 30분 경과 시 세션 종료 이벤트가 발생하고, 세션에 `endTime`이 기록된다.
- Dependencies: Story 1-2 완료

#### Tasks

- [x] 마지막 시청 시각 갱신 로직 구현
  - Priority: High / Owner: FE
  - 내용: 영상 감지 시마다 `lastWatchedAt` 타임스탬프를 storage에 갱신
  - DoD: 영상 감지 시 `lastWatchedAt`이 정확히 업데이트된다.
  - Dependencies: Story 1-2 완료
  - 예상 소요: 1h
  - 설계 결정: `lastWatchedAt`은 `currentSession` 내부가 아닌 `chrome.storage.local`의 독립 키로 저장. 이유: 세션이 null인 상태에서도 마지막 시청 시각 추적이 가능해야 하며, alarm 체크 시 `currentSession` 전체를 로드할 필요 없이 단일 키만 읽으면 됨.
  - 설계 결정: 중복 videoId early return 전에 `lastWatchedAt` 갱신. 이유: 같은 영상을 계속 시청 중인 경우도 활성 시청이므로, 중복 여부와 무관하게 갱신하지 않으면 30분 타임아웃이 잘못 트리거될 수 있음.

- [x] 30분 타임아웃 감지 및 세션 종료 트리거 구현
  - Priority: High / Owner: FE
  - 내용: background service worker에서 `chrome.alarms`로 1분마다 `lastWatchedAt` 확인. 30분 초과 시 세션 종료 이벤트 발생
  - DoD: 마지막 시청 후 30분 후 세션 종료가 자동으로 감지된다.
  - Dependencies: 마지막 시청 시각 갱신 로직 완료
  - 예상 소요: 2~3h
  - 설계 결정: `setInterval` 대신 `chrome.alarms` 사용. 이유: service worker는 이벤트가 없으면 수십 초 내로 종료되므로 `setInterval`은 타이머 유지를 보장하지 않음. `chrome.alarms`는 OS 수준에서 스케줄링되어 service worker를 직접 깨움.
  - 설계 결정: `chrome.alarms.create`를 service worker 최상위(top-level)에서 호출. 이유: service worker는 이벤트가 발생할 때마다 새로 깨어나므로, `onInstalled`·`onStartup`만 사용하면 중간에 alarm이 소실될 수 있음. 같은 이름의 alarm은 자동 교체되므로 중복 등록 문제 없음.

- [x] 세션 시작/종료 시간 및 세션 ID 기록 로직 구현
  - Priority: Medium / Owner: FE
  - 내용: 첫 영상 감지 시 `sessionId`(UUID 또는 타임스탬프), `startTime` 기록. 종료 시 `endTime` 기록
  - DoD: 종료된 세션 객체에 `sessionId`, `startTime`, `endTime`이 모두 포함된다.
  - Dependencies: 30분 타임아웃 감지 완료
  - 예상 소요: 1~2h
  - 설계 결정: `endSession()`에서 `currentSession`과 `sessions`를 단일 `get` 호출로 읽은 뒤, 단일 `set` 호출로 원자적으로 기록. 이유: 두 번의 별도 read/write 사이에 alarm이 재진입하면 세션이 두 번 닫힐 수 있는 레이스 컨디션을 방지.
  - 설계 결정: `currentSession`이 null이거나 `videos`가 빈 배열이면 세션 종료를 스킵. 이유: 영상을 하나도 보지 않은 빈 세션은 분석 대상이 아님.

---

### Story 2-2: 영상 ID로 YouTube Data API를 통해 카테고리 정보를 조회할 수 있다

- 설명: 세션에 포함된 videoId 목록을 YouTube Data API `videos.list`에 전달하여 각 영상의 `categoryId`를 조회한다.
- Priority: High
- Owner: FE
- Definition of Done: videoId 목록 입력 시 각 영상의 categoryId와 카테고리명이 반환된다.
- Dependencies: Story 2-1 완료, YouTube Data API 키 발급

#### Tasks

- [ ] YouTube Data API v3 키 발급 및 환경 설정
  - Priority: High / Owner: FE
  - 내용: Google Cloud Console에서 API 키 발급. manifest.json 또는 config 파일에 환경 분리하여 저장
  - DoD: API 키가 코드에 하드코딩되지 않고 동작한다.
  - Dependencies: 없음
  - 예상 소요: 1h

- [ ] videos.list API 호출 함수 구현
  - Priority: High / Owner: FE
  - 내용: `https://www.googleapis.com/youtube/v3/videos?part=snippet&id={ids}` 호출. videoId 최대 50개 배치 처리
  - DoD: videoId 배열 입력 시 각 영상의 `snippet.categoryId`가 반환된다.
  - Dependencies: API 키 발급 완료
  - 예상 소요: 2~3h

- [ ] categoryId → 카테고리명 매핑 테이블 작성
  - Priority: Medium / Owner: FE
  - 내용: YouTube 공식 카테고리 ID(1~44) 기준으로 한국어 카테고리명 매핑 객체 작성 (`{ "10": "음악", "20": "게임", ... }`)
  - DoD: categoryId 입력 시 한국어 카테고리명이 반환된다.
  - Dependencies: 없음
  - 예상 소요: 1~2h

- [ ] API 에러 처리 및 할당량 초과 대응 로직 구현
  - Priority: Medium / Owner: FE
  - 내용: 403(할당량 초과), 네트워크 오류 시 graceful fallback. 실패한 videoId는 `categoryId: null`로 처리
  - DoD: API 오류 발생 시 앱이 중단되지 않고 null로 처리된다.
  - Dependencies: API 호출 함수 구현 완료
  - 예상 소요: 1~2h

---

### Story 2-3: 세션별 카테고리 분포를 Shannon Entropy로 계산할 수 있다

- 설명: 세션에 포함된 영상들의 카테고리 비율을 계산하고 Shannon Entropy로 편향 수치를 산출한다.
- Priority: High
- Owner: FE
- Definition of Done: 세션 데이터 입력 시 카테고리별 비율 객체와 Entropy 값(소수점 2자리)이 반환된다.
- Dependencies: Story 2-2 완료

#### Tasks

- [ ] 카테고리별 비율 계산 함수 구현
  - Priority: High / Owner: FE
  - 내용: `[{categoryId, ...}, ...]` 입력 → `{ "음악": 0.3, "게임": 0.6, "뉴스": 0.1 }` 형태 반환
  - DoD: 단위 테스트로 비율 합이 1.0임을 검증한다.
  - Dependencies: 카테고리명 매핑 테이블 완료
  - 예상 소요: 1~2h

- [ ] Shannon Entropy 계산 함수 구현
  - Priority: High / Owner: FE
  - 내용: `H = -Σ p(i) × log₂p(i)` 구현. 카테고리가 1개일 때(H=0) 처리 포함
  - DoD: `{ "음악": 0.6, "게임": 0.3, "뉴스": 0.1 }` 입력 시 올바른 H값이 반환된다.
  - Dependencies: 비율 계산 함수 완료
  - 예상 소요: 1~2h

- [ ] 세션 분석 결과 데이터 구조 정의 및 storage 저장
  - Priority: High / Owner: FE
  - 내용: `{ sessionId, startTime, endTime, categoryDistribution, entropy, videoCount }` 구조로 종료된 세션 분석 결과를 storage에 저장
  - DoD: 세션 종료 후 storage에 분석 결과 객체가 저장된다.
  - Dependencies: Entropy 계산 함수 완료
  - 예상 소요: 1~2h

---

### Story 2-4: LLM API를 통해 카테고리 분포 기반 자연어 리뷰를 자동 생성할 수 있다

- 설명: 세션 종료 후 카테고리 분포와 Entropy 값을 LLM에 전달하여 사용자 친화적인 자연어 피드백을 생성한다.
- Priority: High
- Owner: FE
- Definition of Done: 세션 종료 시 2~3문장의 자연어 리뷰가 자동 생성되어 storage에 저장된다.
- Dependencies: Story 2-3 완료

#### Tasks

- [ ] LLM API 선택 및 키 발급
  - Priority: High / Owner: FE
  - 내용: Claude API (claude-haiku-4-5 권장 - 비용 효율) 또는 OpenAI API 선택. API 키를 manifest의 환경 설정에 저장
  - DoD: API 키가 정상 발급되고 간단한 호출 테스트가 성공한다.
  - Dependencies: 없음
  - 예상 소요: 1h

- [ ] 카테고리 분포를 LLM 프롬프트로 변환하는 함수 구현
  - Priority: High / Owner: FE
  - 내용: `{ categoryDistribution, entropy, videoCount, duration }` → 자연어 프롬프트 템플릿 변환. 프롬프트는 한국어 리뷰 요청 포함
  - DoD: 다양한 분포 입력에 대해 의미 있는 프롬프트가 생성된다.
  - Dependencies: LLM API 키 발급 완료
  - 예상 소요: 2~3h

- [ ] LLM API 호출 및 응답 파싱 함수 구현
  - Priority: High / Owner: FE
  - 내용: fetch로 LLM API 호출, 응답 텍스트 추출. 타임아웃(10초) 설정
  - DoD: 프롬프트 입력 시 2~3문장의 한국어 리뷰 텍스트가 반환된다.
  - Dependencies: 프롬프트 변환 함수 완료
  - 예상 소요: 2~3h

- [ ] 리뷰 생성 실패 시 폴백 처리 구현
  - Priority: Medium / Owner: FE
  - 내용: API 오류/타임아웃 시 카테고리 분포 기반 템플릿 문자열로 대체 리뷰 생성
  - DoD: LLM API 실패 시 앱이 중단되지 않고 대체 텍스트가 표시된다.
  - Dependencies: LLM API 호출 함수 완료
  - 예상 소요: 1~2h

---

## Epic 3: 팝업 UI & 시각화

- 설명: 사용자가 확장 프로그램 아이콘을 클릭하면 세션 분석 결과(카테고리 분포 그래프, 자연어 리뷰, 주간 리포트)를 팝업으로 확인할 수 있다.

---

### Story 3-1: 팝업에서 세션의 카테고리 분포를 막대그래프로 확인할 수 있다

- 설명: 가장 최근 세션의 카테고리별 시청 비율이 팝업 UI에 막대그래프로 표시된다.
- Priority: High
- Owner: FE
- Definition of Done: 팝업 열기 시 카테고리명, 비율(%), 막대그래프가 정확히 렌더링된다.
- Dependencies: Story 2-3 완료

#### Tasks

- [ ] popup.html / popup.js 기본 구조 작성
  - Priority: High / Owner: FE
  - 내용: 팝업 레이아웃 HTML 작성. storage에서 최근 세션 데이터 로드하는 초기화 로직 구현
  - DoD: 팝업이 오류 없이 열리고 storage 데이터를 콘솔에서 확인할 수 있다.
  - Dependencies: Story 2-3 완료
  - 예상 소요: 1~2h

- [ ] 카테고리 분포 막대그래프 렌더링 구현
  - Priority: High / Owner: FE
  - 내용: Chart.js 또는 순수 CSS/SVG로 가로 막대그래프 구현. 카테고리명 + 비율(%) 표시
  - DoD: `{ "음악": 0.5, "게임": 0.3, "교육": 0.2 }` 데이터로 그래프가 올바르게 그려진다.
  - Dependencies: popup.html 기본 구조 완료
  - 예상 소요: 3~4h

- [ ] 팝업 CSS 스타일링
  - Priority: Low / Owner: FE
  - 내용: 팝업 너비 350px 기준. 가독성 있는 폰트, 색상 팔레트 적용
  - DoD: 시각적으로 정돈된 UI가 Chrome 팝업에서 렌더링된다.
  - Dependencies: 그래프 렌더링 완료
  - 예상 소요: 2~3h

---

### Story 3-2: 팝업에서 세션 종료 후 자동 생성된 자연어 리뷰를 확인할 수 있다

- 설명: 세션 분석이 완료되면 LLM이 생성한 자연어 리뷰가 팝업에 표시된다.
- Priority: High
- Owner: FE
- Definition of Done: 팝업에 리뷰 텍스트가 표시되고, 리뷰 생성 중에는 로딩 상태가 표시된다.
- Dependencies: Story 2-4 완료, Story 3-1 완료

#### Tasks

- [ ] 리뷰 텍스트 표시 영역 구현
  - Priority: High / Owner: FE
  - 내용: storage의 `review` 필드를 읽어 팝업에 텍스트로 렌더링
  - DoD: 리뷰 텍스트가 팝업에 정상 출력된다.
  - Dependencies: popup.html 기본 구조 완료
  - 예상 소요: 1~2h

- [ ] 리뷰 생성 로딩 상태 UI 구현
  - Priority: Medium / Owner: FE
  - 내용: 세션 종료 후 LLM 응답 대기 중 스피너 또는 "분석 중..." 메시지 표시
  - DoD: 리뷰 미생성 상태에서 팝업 열기 시 로딩 UI가 표시된다.
  - Dependencies: 리뷰 표시 영역 완료
  - 예상 소요: 1~2h

---

### Story 3-3: 주간 리포트를 통해 기간별 카테고리 소비 패턴 변화를 확인할 수 있다

- 설명: 최근 7일간의 누적 세션 데이터를 집계하여 카테고리 분포 변화와 Entropy 추이를 팝업에서 확인할 수 있다.
- Priority: Medium
- Owner: FE
- Definition of Done: 주간 리포트 탭에서 7일치 카테고리 분포와 Entropy 변화 그래프가 표시된다.
- Dependencies: Story 3-2 완료, 7일 이상 누적 데이터 존재

#### Tasks

- [ ] 최근 7일 누적 세션 데이터 집계 함수 구현
  - Priority: Medium / Owner: FE
  - 내용: storage의 sessions 배열에서 최근 7일 데이터 필터링 및 일별 카테고리 비율 집계
  - DoD: 7일치 데이터 입력 시 일별 카테고리 분포 배열이 반환된다.
  - Dependencies: Story 2-3 완료
  - 예상 소요: 2~3h

- [ ] 주간 카테고리 분포 변화 시각화 구현
  - Priority: Medium / Owner: FE
  - 내용: 날짜별 카테고리 비율 스택 바차트 또는 라인차트 구현
  - DoD: 7일치 데이터로 날짜별 변화가 시각적으로 구분된다.
  - Dependencies: 집계 함수 완료
  - 예상 소요: 3~4h

- [ ] Entropy 변화 추이 표시 구현
  - Priority: Medium / Owner: FE
  - 내용: 날짜별 Entropy 값을 라인차트로 표시. 베이스라인(1주차) 대비 변화 강조
  - DoD: Entropy 변화 추이가 라인차트로 표시된다.
  - Dependencies: 주간 분포 시각화 완료
  - 예상 소요: 2~3h

---

## Epic 4: 백엔드 서버 - API & DB

- 설명: 확장 프로그램으로부터 익명화된 세션 분석 결과와 설문 응답을 수신하여 SQLite DB에 저장한다.

---

### Story 4-1: 서버가 익명화된 세션 분석 결과를 수신하여 DB에 저장할 수 있다

- 설명: 확장 프로그램이 세션 종료 후 분석 결과(원시 데이터 제외)를 `POST /api/sessions`로 전송하면 서버가 DB에 저장한다.
- Priority: High
- Owner: BE
- Definition of Done: Postman 또는 curl로 `POST /api/sessions` 요청 시 DB에 레코드가 정상 저장된다.
- Dependencies: `responseHandler.js` 완료, `app.js` 완료

#### Tasks

- [ ] db.js 작성 - SQLite 연결 및 테이블 초기화
  - Priority: High / Owner: BE
  - 내용: `better-sqlite3`로 DB 연결. 서버 시작 시 테이블이 없으면 자동 생성하는 `initializeDB()` 함수 구현
  - DoD: 서버 기동 시 `youtube_bias.db` 파일이 생성되고 테이블이 초기화된다.
  - Dependencies: 없음
  - 예상 소요: 2~3h

- [ ] sessions 테이블 스키마 정의
  - Priority: High / Owner: BE
  - 내용: `id`, `anonymousId`, `sessionId`(UNIQUE), `startTime`, `endTime`, `videoCount`, `categoryDistribution`(JSON TEXT), `entropy`, `createdAt` 컬럼 정의
  - DoD: 스키마가 `db.js`에 정의되고 테이블이 정상 생성된다.
  - Dependencies: db.js 작성 완료
  - 예상 소요: 1~2h

- [ ] POST /api/sessions 라우터 구현
  - Priority: High / Owner: BE
  - 내용: `routes/sessions.js` 작성. 요청 body에서 필드 추출 → DB INSERT → `success()` 응답 반환. `sessionId` 중복 시 `DUPLICATE_SESSION` 에러 반환
  - DoD: 유효한 요청 시 201이 아닌 200으로 성공 응답이 반환되고 DB에 저장된다.
  - Dependencies: sessions 테이블 스키마 완료
  - 예상 소요: 2~3h

- [ ] sessions 라우터 입력값 검증 로직 구현
  - Priority: High / Owner: BE
  - 내용: `anonymousId`, `sessionId`, `startTime`, `endTime` 필수 필드 검증. 누락 시 `MISSING_REQUIRED_FIELD`, 형식 오류 시 `INVALID_FIELD_VALUE` 반환
  - DoD: 각 에러 케이스에 대해 올바른 에러 코드와 HTTP 상태가 반환된다.
  - Dependencies: POST /api/sessions 라우터 완료
  - 예상 소요: 1~2h

---

### Story 4-2: 서버가 설문 응답을 수신하여 DB에 저장할 수 있다

- 설명: 실험 참여자의 사전/사후 설문 응답을 `POST /api/surveys`로 수신하여 DB에 저장한다.
- Priority: High
- Owner: BE
- Definition of Done: `POST /api/surveys` 요청 시 DB에 설문 레코드가 정상 저장된다.
- Dependencies: Story 4-1 완료

#### Tasks

- [ ] surveys 테이블 스키마 정의
  - Priority: High / Owner: BE
  - 내용: `id`, `anonymousId`, `surveyType`(pre/post), `responses`(JSON TEXT), `submittedAt` 컬럼 정의
  - DoD: surveys 테이블이 DB에 정상 생성된다.
  - Dependencies: db.js 작성 완료
  - 예상 소요: 1~2h

- [ ] POST /api/surveys 라우터 구현
  - Priority: High / Owner: BE
  - 내용: `routes/surveys.js` 작성. `anonymousId`, `surveyType`, `responses` 필수 검증 및 DB INSERT
  - DoD: 유효한 요청 시 성공 응답이 반환되고 DB에 저장된다.
  - Dependencies: surveys 테이블 스키마 완료
  - 예상 소요: 2~3h

---

### Story 4-3: 서버를 외부에 배포하여 확장 프로그램과 통신할 수 있다

- 설명: 로컬호스트가 아닌 외부 접근 가능한 서버에 배포하여 실제 실험 환경에서 사용한다.
- Priority: Medium
- Owner: BE
- Definition of Done: 배포된 서버의 `/health` 엔드포인트가 외부에서 200 응답을 반환한다.
- Dependencies: Story 4-2 완료

#### Tasks

- [ ] .env.example 파일 작성
  - Priority: Medium / Owner: BE
  - 내용: `PORT`, `NODE_ENV` 등 필요한 환경변수 목록을 `.env.example`에 작성. `.env`는 `.gitignore`에 추가
  - DoD: `.env.example`을 복사해 `.env`를 구성하면 서버가 기동된다.
  - Dependencies: 없음
  - 예상 소요: 30m~1h

- [ ] 서버 배포 (Render 또는 fly.io)
  - Priority: Medium / Owner: BE
  - 내용: 무료 티어 PaaS에 배포. SQLite 파일 영속성 설정(volume 마운트 또는 persistent disk)
  - DoD: 배포된 URL의 `/health`가 정상 응답한다.
  - Dependencies: .env.example 완료
  - 예상 소요: 2~4h

---

## Epic 5: 사용자 실험 & 연구 검증

- 설명: IRB 승인을 획득하고 실험군/대조군 실험을 진행하여 피드백 시스템의 편향 개선 효과를 검증한다.

---

### Story 5-1: IRB 심사를 통과하기 위한 연구 계획 문서를 제출할 수 있다

- 설명: 연구 목적, 데이터 수집 범위, 저장 방식, 폐기 계획을 명시한 IRB 신청서와 사전 동의서를 작성하여 제출한다.
- Priority: High
- Owner: Research
- Definition of Done: IRB 신청서 및 동의서가 검토 완료되어 제출된다.
- Dependencies: 없음 (개발과 병행)

#### Tasks

- [ ] IRB 신청서 초안 작성
  - Priority: High / Owner: Research
  - 내용: 연구 목적, 참여 대상(만 18세 이상, Chrome 사용자), 수집 데이터 범위(분석 결과만, 원시 데이터 제외), 로컬 저장 원칙, 폐기 계획 명시
  - DoD: 지도교수 또는 IRB 담당자 검토 가능한 초안이 완성된다.
  - Dependencies: 없음
  - 예상 소요: 4h

- [ ] 사전 동의서 작성
  - Priority: High / Owner: Research
  - 내용: 수집 항목, 사용 목적, 자발적 참여 및 중도 탈퇴 방법, 데이터 보존 기간(연구 종료 후 1년), 미성년자 제외 명시
  - DoD: 법적 요건을 충족하는 동의서 양식이 완성된다.
  - Dependencies: IRB 신청서 초안 완료
  - 예상 소요: 2~3h

- [ ] 개인정보 처리 방침 작성
  - Priority: Medium / Owner: Research
  - 내용: 수집 데이터(anonymousId, 카테고리 분포, Entropy), 저장 위치, 제3자 제공 없음 명시
  - DoD: 참여자에게 제공 가능한 개인정보 처리 방침 문서가 완성된다.
  - Dependencies: 없음
  - 예상 소요: 2h

---

### Story 5-2: 실험 참여자를 모집하고 실험군/대조군으로 균형 있게 배치할 수 있다

- 설명: 20~30명의 참여자를 모집하여 사용 시간, 성별 등 개인차가 편중되지 않도록 실험군/대조군에 배치한다.
- Priority: High
- Owner: Research
- Definition of Done: 최소 15명 이상의 참여자가 실험군/대조군에 균형 있게 배치되고 동의서를 제출한다.
- Dependencies: IRB 승인 완료

#### Tasks

- [ ] 모집 공고문 작성 및 배포
  - Priority: High / Owner: Research
  - 내용: 참여 조건(만 18세 이상, Chrome 사용자, 주 1시간 이상 YouTube 시청), 보상(소정의 상품권), 3주 실험 일정 안내
  - DoD: 학과 내 공지 및 지인 네트워크를 통해 공고가 배포된다.
  - Dependencies: IRB 승인 완료
  - 예상 소요: 1~2h

- [ ] 스크리닝 설문 작성 및 배포
  - Priority: High / Owner: Research
  - 내용: 연령, 성별, 주간 YouTube 시청 시간, 주 시청 카테고리 등 수집. Google Forms 활용
  - DoD: 참여 신청자 대상 스크리닝 설문이 배포되고 응답이 수집된다.
  - Dependencies: 모집 공고 배포 완료
  - 예상 소요: 2h

- [ ] 실험군/대조군 배치 기준 설정 및 배치 실행
  - Priority: High / Owner: Research
  - 내용: 성별, 시청 시간, 주 시청 카테고리 기준으로 두 집단이 균형을 이루도록 배치. 배치 결과 스프레드시트 관리
  - DoD: 두 집단 간 주요 변수(성별 비율, 평균 시청 시간)가 유사하게 배치된다.
  - Dependencies: 스크리닝 설문 응답 수집 완료
  - 예상 소요: 1~2h

---

### Story 5-3: 실험 결과를 분석하여 피드백의 편향 개선 효과를 검증할 수 있다

- 설명: 3주 실험 후 수집된 Entropy 데이터와 설문 응답을 분석하여 피드백 제공 여부에 따른 시청 편향 변화를 검증한다.
- Priority: High
- Owner: Research / Data
- Definition of Done: 실험군/대조군 간 Entropy 변화량 차이가 통계적으로 분석되고 결과 보고서가 작성된다.
- Dependencies: 3주 실험 완료, Story 5-2 완료

#### Tasks

- [ ] 사전/사후 설문 문항 설계
  - Priority: High / Owner: Research
  - 내용: (A) 시청 편향 인식 수준, (B) 피드백 유용성/공정성 지각, (C) 피드백 수용도 리커트 척도 문항 작성. 사전(1주차 후)·사후(3주차 후) 동일 문항 사용
  - DoD: 신뢰도 확보를 위한 역코딩 문항 포함, 최종 설문지가 완성된다.
  - Dependencies: 없음
  - 예상 소요: 3~4h

- [ ] 카테고리 Entropy 변화량 분석 스크립트 작성
  - Priority: High / Owner: Data
  - 내용: DB에서 참여자별 주차별 Entropy 추출. 베이스라인(1주차) 대비 2~3주차 변화량(Δ Entropy) 계산. Python 또는 R 사용
  - DoD: 참여자별 Δ Entropy 표와 실험군/대조군 평균 비교 결과가 출력된다.
  - Dependencies: 3주 실험 완료
  - 예상 소요: 3~4h

- [ ] 실험군/대조군 비교 통계 분석 실행
  - Priority: High / Owner: Data
  - 내용: 독립표본 t-검정 또는 Mann-Whitney U 검정으로 두 집단의 Δ Entropy 차이 검증. 설문 응답 분석 포함
  - DoD: p-value, 효과 크기(Cohen's d)가 산출된 통계 분석 결과가 문서화된다.
  - Dependencies: Entropy 분석 스크립트 완료
  - 예상 소요: 4h

- [ ] 반구조화 인터뷰 가이드 작성 및 인터뷰 진행
  - Priority: Medium / Owner: Research
  - 내용: 실험 종료 후 5~10명 대상. 피드백 유용성, 리뷰 자연스러움, 행동 변화 경험 등 질문 가이드 작성 및 진행
  - DoD: 인터뷰 가이드가 완성되고 5명 이상의 인터뷰 녹취록이 확보된다.
  - Dependencies: 3주 실험 완료
  - 예상 소요: 가이드 2~3h + 인터뷰 진행 별도
