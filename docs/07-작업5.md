# 작업 구조 (5차)

`docs/blog/대조군-연구종료-리뷰열람-명세서.md`(Story 10-10 "대조군 일괄 피드백 공개" 하위 범위의 구체화)를 기준으로, 대조군 연구 종료 후 리뷰 열람 기능을 이슈 4개로 나눠 진행한다. 이 작업은 `Epic 10`/`Epic 11`처럼 번호가 매겨진 Epic에 속하지 않고, **"1차 테스트 이후 개발"**로 분류한다 — 파일럿(1차 테스트) 운영 중 발견된 후속 필요 기능이기 때문이다.

각 이슈는 `docs/blog/이슈-초안-대조군-연구종료-리뷰열람.md`에 `.github/ISSUE_TEMPLATE/feat.md` 양식으로 전체 초안이 작성돼 있다. 이 문서는 실제 이슈 본문을 반복하지 않고, 착수 순서·의존관계·설계 결정 요약만 추적한다.

---

## Epic: 1차 테스트 이후 개발

- 설명: 대조군(CON)은 개입 기간 중 시청 현황 요약(`screenControlHome`)만 보고 실제 AI 피드백을 받지 못한다. IRB 신청서 §11(2)다), §11(3)에 "연구 종료 후 6주간 누적 시청 기록 분석·피드백을 일괄 제공한다"고 서약된 내용을 구현한다(Story 10-10 소속, `docs/05-작업3.md`).
- 진행 현황: **이슈 1·2 완료**. 이슈 3~4는 미착수(이슈 3 착수 준비 중).
- 착수 순서: 이슈 1 → 이슈 2 → 이슈 3 (직렬 의존). 이슈 4는 독립적으로 병행 가능하나, 이슈 3의 `buildWeeksData` 폴백과 함께 있어야 "재설치 후 6주 리뷰 정상 표시" 시나리오가 완결된다.
- 공통 선행 조건(모든 이슈 착수 전 확인 필요):
  - **IRB 승인 범위 확인**: 대조군 `period_reviews` 사전 생성(이슈 1)이 "언제 처리되는가"까지 IRB 승인 범위에 포함되는지 문면 확인 — **완료(승인 범위 확인됨)**.
  - **배포 전 기존 중복 데이터 점검**: 이 기능 배포 이전에 이미 같은 참여코드로 재설치가 발생해 `participants`에 중복 행이 쌓여 있는지 확인(이슈 4) — 미착수.
  - **Dependencies(Story 10-10 원본과 동일)**: Story 10-8(베이스라인·타임라인 상수, 완료됨) 필요. Story 10-9(설문 문항 확정)는 이 작업 범위와 무관 — 설문 배너는 Story 10-10의 별도 하위 작업.
- 관련 명세서: `docs/blog/대조군-연구종료-리뷰열람-명세서.md`
- 관련 문서: `docs/05-작업3.md` Story 10-10, `docs/06-작업4.md` Story 11-1(`period_reviews` 원본 설계)

---

### 이슈 1: [서버] 대조군 `period_reviews` 사전 생성 활성화

- 설명: `generate-period-reviews.js`의 `ELIGIBLE_GROUPS`가 지금 `["EXP","TEST-EXP"]`로 대조군을 명시적으로 제외하고 있다. 이를 확장해 대조군도 실험군과 동일한 주기·동일한 파이프라인으로 `period_reviews`가 조용히(비공개로) 쌓이게 한다. 연구 종료 시점에 별도로 소급·일괄 생성하는 로직은 만들지 않는다.
- Priority: High / Owner: BE
- Dependencies: 없음 — 가장 먼저 착수
- 상태: **완료**

#### 설계 결정 (배경)

- **사전 생성 vs 소급 생성 — 사전 생성으로 확정(사용자 결정)**: 기존 cron 인프라를 100% 재사용(배열 수정 1줄)하고, 연구 종료일에 전원(EXP+CON)의 밀린 기간이 한꺼번에 몰리는 부하를 피할 수 있다. `period_reviews` 스키마는 애초에 "그룹 무관하게 일반적으로 설계"돼 있어(기간-리뷰-생성-명세서.md 10절) 설계 의도와도 맞는다.
- **상수 중앙화**: `TOTAL_DAYS`/`DAYS_PER_PERIOD`/`BASELINE_DAYS`가 지금 `generate-period-reviews.js` 파일 안에만 있어, 이슈 2가 새로 만들 서버 함수(`isStudyEndUnlocked`)가 이 값을 또 하드코딩하면 서버 파일 2개 사이에서도 값이 어긋날 위험이 생긴다. `server/pipeline/study-constants.js`(신규)로 뽑아 서버 내부 단일 정의로 만든다 — 본조사 전환(파일럿값 → 실제 6주 값) 시 상수만 바꾸면 되도록.
- 프롬프트·가드레일·fallback 로직(`server/pipeline/period-review-llm.js`) 자체는 변경하지 않는다 — 대상 그룹만 확장.

#### Tasks

- [x] `server/pipeline/study-constants.js`(신규) — `TOTAL_DAYS`/`DAYS_PER_PERIOD`/`BASELINE_DAYS` 단일 정의
- [x] `server/scripts/generate-period-reviews.js` — 상수 import로 교체, `ELIGIBLE_GROUPS`에 `CON`/`TEST-CON` 추가. `server/test/scripts/generate-period-reviews.test.js`의 "대조군은 제외된다" 테스트를 "대조군도 포함된다"로 교체(회귀 없음, 81개 테스트 통과)
- [x] IRB 승인 범위 확인(Epic 공통 선행 조건과 동일 항목) — 확인됨

- 관련 문서: `docs/blog/대조군-연구종료-리뷰열람-명세서.md` 4-1절, 4-4절 / `docs/blog/이슈-초안-대조군-연구종료-리뷰열람.md` 이슈 1

---

### 이슈 2: [서버] 대조군 6주 리뷰 열람 접근 제어 + 열람 이벤트 로깅

- 설명: 이슈 1로 대조군의 `period_reviews`가 쌓이기 시작해도, `period-reviews-query.js`의 `FEEDBACK_GROUPS` 필터가 대조군을 무조건 빈 배열로 막는다. 이 방어를 "연구 종료 + 전체 기간 생성 완료" 조건에서만 허용하도록 바꾼다. 이 리포에서 **가장 민감한 충돌 지점**이다 — 조건 계산이 잘못되면 처치 오염(연구 종료 전 대조군에게 리뷰 유출) 위험이 그대로 있다.
- Priority: High / Owner: BE
- Dependencies: 이슈 1(대조군 `period_reviews`가 실제로 있어야 접근 제어 검증 가능)
- 상태: **완료**

#### 설계 결정 (배경)

- **트리거 판정 — 상태 기반(생성 완료 여부) + 시간 하한을 함께 게이트**: 순수 시간 기반("종료 다음날 자동")은 채택하지 않는다. cron이 매일 04:00 KST 1회만 실행돼 마지막 기간 완료 후 행이 생기기까지 최대 약 28시간 지연될 수 있고, 처리 중 예외가 나면 하루 더 밀릴 수 있기 때문이다. `해당 참여자의 period_reviews 행 개수 == totalPeriods`가 정확히 "모든 기간의 생성이 끝났다"는 뜻이므로 이를 그대로 트리거 조건으로 쓴다(성공/fallback 여부 무관).
- 신규 컬럼은 `participants`에 2개만 추가(신규 테이블 없음) — 참여자 1명당 정확히 1번 발생하는 이벤트라 `installDate`와 같은 성격으로 판단.

#### Tasks

- [x] `isStudyEndUnlocked(db, anonymousId, installDate)`/`isStudyEnded(installDate)` 함수 추가(`study-constants.js` 상수 재사용, `server/routes/period-reviews-query.js`)
- [x] `server/routes/period-reviews-query.js` — `FEEDBACK_GROUPS` 조건에 대조군 조건부 허용 추가(`server/test/routes/period-reviews.test.js`에 종료 전/미완성/완성 3케이스 + EXP 회귀 테스트 반영)
- [x] `server/db.js` — `participants`에 `studyEndModalShownAt`/`studyEndReviewViewedAt` 컬럼 추가(`CREATE TABLE` + `addColumn` 이중 반영, 로컬 개발 DB에 마이그레이션 적용·재실행 멱등성 확인)
- [x] `POST /api/participants/study-end-review-event` 신규(`server/routes/study-end-review-event.js`의 `recordStudyEndReviewEvent`, `COALESCE`로 최초 1회만 기록. 단위 테스트 5개 `server/test/routes/study-end-review-event.test.js`)

- 관련 문서: `docs/blog/대조군-연구종료-리뷰열람-명세서.md` 2절, 4-4·4-5절, 5절, 7절(충돌 지점 1번) / `docs/blog/이슈-초안-대조군-연구종료-리뷰열람.md` 이슈 2

---

### 이슈 3: [클라이언트] 종료 안내 모달 + 6주 누적 리뷰 화면

- 설명: 대조군 참여자가 실제로 6주 리뷰를 볼 수 있는 화면과 진입 흐름을 만든다. 조건 충족 시 모달을 최초 1회만 띄우고, 이후엔 `screenControlHome`에 상시 CTA 버튼으로 재진입할 수 있게 한다. "지금 확인하기"를 누르면 기존 실험군 주차별 탭 인프라(`buildWeeksData`/`screenFeedback`)를 재사용한다.
- Priority: High / Owner: FE
- Dependencies: 이슈 2(열람 API가 열려 있어야 실 데이터로 화면을 붙여 테스트 가능)
- 상태: 미착수

#### 설계 결정 (배경)

- **모달 재노출 정책 — 최초 1회만 모달, 이후 상시 CTA(사용자 결정)**: 매번 모달을 띄우면 참여자 경험상 성가시고, 이 리포에는 반복 모달 패턴이 없다(세션 리뷰 확인도 "한 번 확인하면 끝"). "예/아니오"는 지금 볼지 나중에 볼지의 선택일 뿐이라 상시 진입점을 남긴다.
- **`GROUPS.CON.feedback` 플래그는 바꾸지 않는다**: 이 플래그를 바꾸면 `_isFeedbackActive`를 통해 세션 리뷰 블러/확인 로직(핵심 연구 측정 신호)까지 오작동할 위험이 있다. "연구 종료 여부"라는 새 축을 `screenControlHome` 내부에 독립적으로 추가하는 방식으로 구현한다.
- **`buildWeeksData` 폴백 분기 필요**: 지금 주차별 분포·엔트로피는 서버 스냅샷이 아니라 로컬 세션 캐시에서 매번 재계산된다. 재설치(이슈 4)로 로컬 캐시가 비면 `anonymousId`를 복구해도 화면이 빈 값으로 보인다 — 로컬 세션이 없을 때 `periodReviews` 행의 스냅샷 값을 그대로 쓰는 폴백을 추가한다. 실험군 경로는 이 폴백을 타지 않아 기존 동작이 그대로 유지되는 순수 추가(additive) 변경이다.

#### Tasks

- [ ] `_isStudyEndReviewReady(groupCfg, installDate, periodReviews)` 게이트 함수
- [ ] 종료 안내 모달 UI + 1회성 노출 로직
- [ ] `screenControlHome` 상시 CTA 카드(모달을 이미 본 뒤)
- [ ] 6주 누적 리뷰 화면 — `buildWeeksData`+`screenFeedback` 연결
- [ ] `buildWeeksData` 폴백 분기(로컬 세션 없을 때 서버 스냅샷 사용)
- [ ] `POST /api/participants/study-end-review-event` 연동(모달 노출/리뷰 열람 시점)

- 관련 문서: `docs/blog/대조군-연구종료-리뷰열람-명세서.md` 2·3절, 4-2절, 5절(클라이언트), 6절 / `docs/blog/이슈-초안-대조군-연구종료-리뷰열람.md` 이슈 3
- 문구(모달·CTA·화면 헤더)는 개발 중 확정(사용자 결정, 명세서 3절은 자리표시용 초안)

---

### 이슈 4: [클라이언트+서버] 참여코드 기반 재설치 복구

- 설명: `anonymousId`가 로컬(`chrome.storage.local`)에만 생성돼, 확장 재설치 시 새 참여자로 취급된다. 실험군에게도 문제지만 대조군에게는 특히 치명적이다 — 재설치하면 6주 누적 리뷰를 영영 못 받는 형태로 IRB 서약("일괄 제공")이 깨진다. 참여코드로 "이전에 설치한 적이 있는 [코드]님이 맞습니까?" 확인 후, 맞으면 기존 `anonymousId`/`installDate`를 복구한다.
- Priority: Medium / Owner: FE + BE
- Dependencies: 없음(독립 개발 가능). 이슈 3의 `buildWeeksData` 폴백과 함께 있어야 "재설치 후 6주 리뷰 정상 표시"까지 엔드투엔드 검증됨. 파일럿(1차 테스트) 종료 전 완료 권장.
- 상태: 미착수

#### 설계 결정 (배경)

- **참여코드를 사실상의 인증 수단으로 재사용**: 발급 명단(`issued_codes`)으로 온보딩 자체를 이미 통제하고 있으므로, 복구 시 같은 코드를 다시 요구하는 것은 기존 보안 모델과 일관된다. 별도의 강한 인증을 새로 설계하지 않는다.
- **`/recover`가 `installDate ASC` 첫 행을 반환**: 이 기능 배포 전 같은 참여코드로 이미 여러 번 재설치가 일어났을 가능성을 배제할 수 없다 — "가장 먼저 등록된" 행이 원래 베이스라인 시작일을 담고 있을 가능성이 가장 높다는 실용적 절충이다. 완전한 해법은 아니므로 배포 전 데이터 점검(Epic 공통 선행 조건)이 필요하다.
- **로컬 `chrome.storage.local.sessions`는 복구 범위 밖**: 서버 쪽 `anonymousId`/`installDate`/`period_reviews`는 복구되지만, 실험군의 "오늘"·진행 중 주차 화면이 의존하는 로컬 세션 캐시는 애초에 서버에 그 형태로 보관돼 있지 않아 복구 불가 — 대조군 6주 리뷰 화면은 이슈 3의 폴백 덕분에 이 한계의 영향을 받지 않는다.

#### Tasks

- [ ] `GET /api/participants/validate` 응답에 `previouslyRegistered` 필드 추가(TEST 코드 제외)
- [ ] `POST /api/participants/recover` 신규
- [ ] 온보딩 확인 모달("이전에 설치한 적이 있는 [코드]님이 맞습니까?")
- [ ] 클라이언트 — "예" 선택 시 로컬 스토리지 덮어쓰기(신규 `POST /api/participants` 재호출 생략)
- [ ] 배포 전 기존 중복 등록 데이터 점검(Epic 공통 선행 조건과 동일 항목)

- 관련 문서: `docs/blog/대조군-연구종료-리뷰열람-명세서.md` 4-3절, 6절, 7절(충돌 지점 5번) / `docs/blog/이슈-초안-대조군-연구종료-리뷰열람.md` 이슈 4
